# Core scraper dependencies
pyktok
playwright
browser-cookie3
beautifulsoup4
numpy
pandas
requests
psutil>=5.9.0

# Video analysis dependencies (required)
opencv-python-headless>=4.8.0
moviepy>=1.0.3
scikit-image>=0.21.0

# Optional: For better face/person detection (requires Python <3.13)
# mediapipe>=0.10.0

# For GPU-accelerated person detection (extreme/maximum presets)
ultralytics>=8.0.0

# OCR dependencies (for tiktok_studio_ocr.py)
google-cloud-vision>=3.5.0
pillow
# pytesseract  # Optional: kept for fallback mode when Vision API fails

# Web API dependencies (for FastAPI backend)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9
websockets>=13.0